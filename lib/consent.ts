// Shared between components/cookie-banner.tsx and components/meta-pixel.tsx
// so both agree on the same localStorage key and event name.
export const COOKIE_CONSENT_KEY = 'diros-cookie-consent'
export const COOKIE_CONSENT_EVENT = 'diros-cookie-consent-accepted'
